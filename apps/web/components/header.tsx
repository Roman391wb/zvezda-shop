"use client";
import {useEffect,useState} from "react";
import Link from "next/link"; import {usePathname} from "next/navigation"; import {Heart,MagnifyingGlass,ShoppingBag,List,X} from "@phosphor-icons/react"; import {useStore} from "./store-provider";
const links=[["Каталог","/catalog"],["Новинки","/catalog?sort=newest"],["Распродажа","/catalog?category=sale"],["О магазине","/about"],["Доставка","/delivery"],["Контакты","/contacts"]] as const;
export function Header({storeName}:{storeName?:string}){const s=useStore(),count=s.cart.reduce((n,x)=>n+x.quantity,0),[menuOpen,setMenuOpen]=useState(false),[atTop,setAtTop]=useState(true),name=storeName||process.env.NEXT_PUBLIC_STORE_NAME,pathname=usePathname();useEffect(()=>{const update=()=>setAtTop(window.scrollY<16);update();window.addEventListener("scroll",update,{passive:true});return()=>window.removeEventListener("scroll",update)},[]);return <header className={`header ${pathname==="/"&&atTop?"header-over-hero":""}`}><div className="wrap header-inner"><nav className="nav">{links.slice(0,4).map(([label,href])=><Link key={href} href={href}>{label}</Link>)}</nav><button className="icon-btn mobile-menu" aria-label={menuOpen?"Закрыть меню":"Открыть меню"} aria-expanded={menuOpen} onClick={()=>setMenuOpen(v=>!v)}>{menuOpen?<X size={23}/>:<List size={23}/>}</button><Link className={`logo ${!name?"logo-empty":""}`} href="/" onClick={()=>setMenuOpen(false)} aria-label={name||"Главная"}>{name}</Link><div className="actions"><button className="icon-btn" aria-label="Поиск" onClick={()=>s.setSearchOpen(true)}><MagnifyingGlass size={22}/></button><Link className="icon-btn" href="/favorites" aria-label="Избранное"><Heart size={22}/>{s.favorites.length>0&&<span className="count">{s.favorites.length}</span>}</Link><button className="icon-btn" aria-label="Корзина" onClick={()=>s.setCartOpen(true)}><ShoppingBag size={22}/>{count>0&&<span className="count">{count}</span>}</button></div></div>{menuOpen&&<nav className="mobile-nav" aria-label="Мобильное меню">{links.map(([label,href])=><Link key={href} href={href} onClick={()=>setMenuOpen(false)}>{label}</Link>)}<Link href="/returns" onClick={()=>setMenuOpen(false)}>Возврат и обмен</Link></nav>}</header>}
